Add cross-platform ability to detect the shift and control keys held down while moving.

Find a number pad and test/get it to work on linux/windows.

Figure out how to resize the console on windows. 

Include a "wilderness" generator.

Maze generators should be loaded like DBI modules... with "genmaze" taking a "method" argument, which coresponds to a .pm module sitting in a directory somewhere.


Get Save/Load world to work on Win32
